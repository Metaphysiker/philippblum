<div class="page-header"><h1>Upload PDF</h1></div>

<%= form_for @pdf, html: { multipart: true } do |f| %>
    <% if @pdf.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= "#{pluralize(@pdf.errors.count, "error")} prohibited this photo from being saved:" %>
          </h2>
          <ul>
            <% @pdf.errors.full_messages.each do |msg| %>
                <li>
                  <%= msg %>
                </li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :file %>
      <%= f.file_field :file, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.submit 'Upload PDF', class: 'btn btn-default' %>
    </div>
<% end %>